import "./loader.scss";

const Loader = () => {
  return (
    <section className="loader">
      <img className="loader__image" src="./loader.png" alt="loader" />
    </section>
  );
};

export default Loader;
